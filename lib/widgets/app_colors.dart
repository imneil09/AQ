import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF6366F1);      // Indigo
  static const background = Color(0xFF0F172A);   // Dark Slate
  static const surface = Color(0xFF1E293B);      // Lighter Slate
  static const error = Color(0xFFF43F5E);        // Rose
  static const success = Color(0xFF10B981);      // Emerald

  // Reusable Opacity Styles
  static final glassWhite = Colors.white.withOpacity(0.05);
  static final glassBorder = Colors.white.withOpacity(0.1);
}